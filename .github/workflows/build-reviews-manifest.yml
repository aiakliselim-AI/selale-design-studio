name: build-reviews-manifest

on:
  push:
    branches: [main]
    paths:
      - 'reviews/**'
      - 'scripts/build-reviews-manifest.mjs'
      - '.github/workflows/build-reviews-manifest.yml'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: build-reviews-manifest
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install js-yaml
        run: npm install --no-save --silent js-yaml@4

      - name: Build manifest
        run: node scripts/build-reviews-manifest.mjs

      - name: Manifest değiştiyse PR aç ve otomatik birleştir
        # NOT: [skip ci] kullanma — Cloudflare Pages deploy'u atlar.
        # Doğrudan push yerine PR akışı: main-protection ruleset ile uyumlu.
        # Loop guard: on.push.paths filtresi data/reviews.json içermiyor,
        # bu PR merge edildiğinde workflow yeniden tetiklenmez.
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "data/reviews.json: müşteri yorumları manifesti güncellendi"
          title: "data/reviews.json: müşteri yorumları manifesti güncellendi"
          body: "Otomatik manifest güncellemesi — `build-reviews-manifest` workflow tarafından üretildi."
          branch: ci/update-reviews-manifest
          base: main
          delete-branch: true
          add-paths: data/reviews.json
          auto-merge: squash

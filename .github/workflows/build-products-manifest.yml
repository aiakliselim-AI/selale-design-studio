name: build-products-manifest

on:
  push:
    branches: [main]
    paths:
      - 'products/**'
      - 'scripts/build-products-manifest.mjs'
      - '.github/workflows/build-products-manifest.yml'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: build-products-manifest
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install js-yaml
        run: npm install --no-save --silent js-yaml@4

      - name: Build manifest
        run: node scripts/build-products-manifest.mjs

      - name: Manifest değiştiyse PR aç ve otomatik birleştir
        # NOT: [skip ci] kullanma — Cloudflare Pages deploy'u atlar.
        # Doğrudan push yerine PR akışı: main-protection ruleset ile uyumlu.
        # Loop guard: on.push.paths filtresi data/products.json içermiyor,
        # bu PR merge edildiğinde workflow yeniden tetiklenmez.
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "data/products.json: ürün manifesti güncellendi"
          title: "data/products.json: ürün manifesti güncellendi"
          body: "Otomatik manifest güncellemesi — `build-products-manifest` workflow tarafından üretildi."
          branch: ci/update-products-manifest
          base: main
          delete-branch: true
          add-paths: data/products.json
          auto-merge: squash
